# Aetheria Hyper-GT Render Deployment Blueprint
services:
  - type: web
    name: aetheria-hyper-gt
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
